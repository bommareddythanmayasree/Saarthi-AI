HERO BACKGROUND IMAGE SETUP
================================

The image you provided (people collaborating at a table) needs to be saved as:
static/images/hero-bg.jpg

STEPS TO ADD THE IMAGE:
1. Save the image you provided in the chat as "hero-bg.jpg"
2. Place it in the "static/images/" folder
3. Recommended specifications:
   - Format: JPG or PNG
   - Recommended size: 1920x1080 or larger
   - The image will be displayed with:
     * Low opacity overlay (85%)
     * Slight blur effect (3px)
     * Cover sizing (fills the hero section)
     * Center positioning

CURRENT CSS CONFIGURATION:
✅ Background image path: url('../images/hero-bg.jpg')
✅ Background size: cover
✅ Background position: center
✅ Light mode overlay: rgba(255,255,255,0.85) with blur(3px)
✅ Dark mode overlay: rgba(10,10,15,0.85) with blur(3px)
✅ Hero text positioned with z-index for readability

The CSS is already configured and ready to use the image once you save it!
